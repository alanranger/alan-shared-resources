# page segmentation by tier.csv — FROZEN historical record

**FROZEN 2026-07-19** — runtime truth is Supabase `pages_master` via `/api/aigeo/pages-master-tier-lookup` (and `lib/pagesMaster.js` / `api/aigeo/tier-segmentation.js`).

This file is **historical reference only**. Do **not** regenerate, overwrite, or wire it back into live dashboard / API paths.

Same freeze pattern as `README-07-url-target-keywords-seospace.md` (seospace CSV superseded by DB overrides + `09-url-target-keywords.csv` export).
